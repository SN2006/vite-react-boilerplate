# Task Types Management Feature

Полная функциональность управления типами задач (Task Types) с отдельной страницей.

## Что было реализовано

### 1. **Исправлены хуки** (`src/features/taskTypes/api.ts`)

Исправлены хуки для работы с типами задач:
- `useMyTaskTypes()` - получение всех типов задач
- `useCreateTaskType()` - создание нового типа
- `useUpdateTaskType()` - редактирование типа
- `useDeleteTaskType()` - удаление типа

Все хуки теперь правильно используют `useMutation` и автоматически обновляют данные.

### 2. **Модальные окна** (`src/components/taskTypes/`)

#### TaskTypeModal.tsx
- Универсальное модальное окно для создания и редактирования типов задач
- React Hook Form с Zod валидацией
- Примеры в placeholder: "Bug", "Feature", "Enhancement"
- Поддержка начальных значений для редактирования

#### DeleteTaskTypeModal.tsx
- Модальное окно подтверждения удаления
- Предупреждение о последствиях (переназначение задач)
- Защита от случайного удаления

### 3. **Страница Task Types** (`src/pages/TaskTypes.tsx`)

Полноценная страница управления типами задач с:
- 📋 **Таблицей** со всеми типами задач
- 🔍 **Просмотром** всех типов с информацией:
  - ID типа
  - Название типа (с красивым бейджем)
  - Владелец (аватар + имя/email)
- ✏️ **Редактированием** - кнопка в каждой строке
- 🗑️ **Удалением** - кнопка в каждой строке
- ➕ **Созданием** - кнопка в хедере
- ⬅️ **Возвратом на Dashboard** - кнопка со стрелкой

### 4. **Навигация**

- Добавлена кнопка **"Task Types"** в хедере Dashboard
- Защищенный роут `/task-types` (требует аутентификации)
- Кнопка "Back to Dashboard" на странице Task Types

## Использование

### Просмотр типов задач

1. На Dashboard нажмите кнопку **"Task Types"** в хедере
2. Откроется страница со таблицей всех типов задач
3. В таблице отображается:
   - ID типа
   - Название (в виде цветного бейджа)
   - Информация о владельце

### Создание нового типа задачи

1. На странице Task Types нажмите **"Add Task Type"**
2. Введите название (например: "Bug", "Feature", "Enhancement")
3. Нажмите **"Save"**
4. Новый тип появится в таблице

### Редактирование типа задачи

1. В таблице найдите нужный тип
2. Нажмите иконку **✏️ (Edit)** справа в строке
3. Измените название
4. Нажмите **"Save"**
5. Все задачи с этим типом автоматически обновятся

### Удаление типа задачи

1. В таблице найдите тип, который хотите удалить
2. Нажмите иконку **🗑️ (Delete)** справа в строке
3. Прочитайте предупреждение
4. Подтвердите удаление кнопкой **"Delete"**
5. ⚠️ **Внимание:** Задачи с этим типом нужно будет переназначить

### Возврат на Dashboard

- Нажмите иконку **⬅️ (стрелка назад)** в левом верхнем углу
- Или используйте кнопку "Logout" для выхода

## API Endpoints

Используются следующие эндпоинты:

- **GET** `/task-types/my` - получение моих типов задач
- **POST** `/task-types` - создание типа задачи
  ```json
  { "name": "Bug" }
  ```
- **PATCH** `/task-types/:id` - обновление типа задачи
  ```json
  { "name": "Feature Request" }
  ```
- **DELETE** `/task-types/:id` - удаление типа задачи

## Валидация

### Правила для названия типа задачи:
- ✅ Обязательное поле
- ✅ Минимум 1 символ
- ✅ Максимум 50 символов
- ❌ Пустые строки не разрешены

## Автоматическое обновление

После любой операции (создание/редактирование/удаление):
- ✅ Автоматически обновляются типы задач
- ✅ Автоматически обновляются задачи на Dashboard
- ✅ Автоматическое перенаправление на `/dashboard`
- ✅ Модальное окно закрывается

## UI/UX особенности

### Таблица
- Адаптивный дизайн
- Hover эффект на строках
- Иконки для действий (редактирование/удаление)
- Цветные бейджи для названий типов
- Аватары владельцев с инициалами

### Состояния загрузки
- Спиннер при загрузке данных
- Кнопки показывают "Saving..." или "Deleting..."
- Блокировка кнопок во время операций

### Цветовая индикация
- **Синие бейджи** - названия типов задач
- **Синяя иконка** - Edit (редактирование)
- **Красная иконка** - Delete (удаление)
- **Синяя кнопка** - Add Task Type (создание)

### Empty State
- Красивый empty state когда нет типов
- Иконка и описание
- Кнопка для создания первого типа

## Навигация

```
Dashboard → [Task Types button] → Task Types Page
    ↑                                      ↓
    └──────── [Back arrow] ────────────────┘
```

## Интеграция с Dashboard

### На Dashboard видно:
- Кнопка "Task Types" для перехода на страницу управления
- В карточках задач отображается тип (берется из TaskType)
- Бейдж с типом задачи (например: "Bug", "Feature")

### Связь с задачами:
- Каждая задача имеет поле `taskType` с ID и именем
- При удалении типа задачи нужно переназначить тип всем связанным задачам
- При изменении имени типа автоматически обновляются все задачи

## Примеры типичных типов задач

Рекомендуемые типы для начала работы:
- 🐛 **Bug** - ошибки и баги
- ✨ **Feature** - новые функции
- 🔧 **Enhancement** - улучшения
- 📝 **Task** - обычные задачи
- 🚀 **Release** - релизы
- 📚 **Documentation** - документация
- 🧪 **Test** - тестирование
- 🔐 **Security** - безопасность

## Структура файлов

```
src/
├── components/
│   └── taskTypes/
│       ├── TaskTypeModal.tsx           # Модал создания/редактирования
│       └── DeleteTaskTypeModal.tsx     # Модал удаления
├── features/
│   └── taskTypes/
│       ├── api.ts                      # Хуки для CRUD операций (исправлены)
│       └── types.ts                    # TypeScript типы
├── pages/
│   ├── Dashboard.tsx                   # Добавлена кнопка Task Types
│   └── TaskTypes.tsx                   # Новая страница управления типами
└── routes/
    └── task-types.tsx                  # Роут /task-types
```

## Преимущества данного решения

### ✅ Отдельная страница
- Не загромождает Dashboard
- Фокус на управлении типами
- Больше места для информации

### ✅ Таблица вместо карточек
- Компактное отображение
- Легко сканировать глазами
- Простое сравнение типов
- Удобные действия в каждой строке

### ✅ Полный CRUD
- Создание (Create)
- Просмотр (Read)
- Редактирование (Update)
- Удаление (Delete)

## Возможные улучшения

В будущем можно добавить:
1. **Иконки для типов** - кастомные иконки (🐛, ✨, 🔧)
2. **Цветовая схема** - настраиваемые цвета для типов
3. **Сортировка** - по ID, имени, владельцу
4. **Фильтрация** - поиск по имени
5. **Пагинация** - для большого количества типов
6. **Статистика** - количество задач по каждому типу
7. **Drag & Drop** - изменение порядка типов
8. **Импорт/Экспорт** - шаблоны типов задач
9. **Архивация** - вместо удаления
10. **История** - логирование изменений

## Горячие клавиши (в будущем)

Можно добавить:
- `Ctrl/Cmd + K` - быстрый поиск типа
- `Ctrl/Cmd + N` - создать новый тип
- `E` - редактировать (при фокусе на строке)
- `Del` - удалить (при фокусе на строке)
- `Esc` - закрыть модальное окно
- `B` - вернуться на Dashboard

## Тестирование

Рекомендуется протестировать:
1. ✅ Создание типа с валидным именем
2. ✅ Создание типа с пустым именем (ошибка)
3. ✅ Создание типа с очень длинным именем (ошибка)
4. ✅ Редактирование существующего типа
5. ✅ Удаление типа с подтверждением
6. ✅ Отмена удаления
7. ✅ Навигация Dashboard ↔ Task Types
8. ✅ Работа без типов (empty state)
9. ✅ Состояния загрузки
10. ✅ Обработка ошибок API

